# Terraform
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfvars
!terraform/terraform.tfvars.example
.terraform.lock.hcl
terraform/*.zip

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# AWS credentials ??? never commit these
.aws/
credentials

# Model artifacts and data files
*.pt
*.pth
*.ckpt
*.joblib
ml/model/model_metadata.json
ml/model/multi_seed_results.json
ml/model/chaos_threshold_sweep.json
mlruns/
ml/data/*.csv
ml/data/*.png
ml/data/labels/

# Misc
.DS_Store
.env
.env.local
*.log

# Docker image tarballs (transferred between machines, not tracked in git)
*.tar
*.tar.gz
*.zip

# Executables and Binaries
*.exe
minikube.exe
terraform.exe

.terraform.tfstate.lock.info
.terraform/

# Benchmark (Track 2) — cached dataset + raw per-run trial data are large/transient;
# summary_*.csv and figures/ (the actual deliverable) stay tracked.
benchmark/data/
benchmark/results/raw/
benchmark/results/raw_stress/
